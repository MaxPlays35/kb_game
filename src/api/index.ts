// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { io, Socket } from "socket.io-client";

export class Api {
  connecion: string;

  constructor() {
    this.connecion = "dfsa";
    console.log(3123);
  }
}
